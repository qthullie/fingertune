# Single-file build (`npm run build:standalone`).
# The HTML file is self-contained, but the MediaPipe wasm and model cannot be
# inlined into it, so they come from public CDNs.
VITE_MEDIAPIPE_WASM_PATH=https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.14/wasm
