# Build single-file (`npm run build:standalone`).
# Le fichier HTML est autonome, mais le wasm et le modele MediaPipe ne peuvent pas
# y etre inlines : ils viennent donc de CDN publics.
VITE_MEDIAPIPE_WASM_PATH=https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.14/wasm
